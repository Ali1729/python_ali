import pygame
pygame.init

pygame.display.set_mode # just for setting the size of the game window
pygame.display.set_mode.fill
pygame.font.SysFont
pygame.time.Clock()  -- how many frames 

pygame.display.set_caption 
pygame.display.flip # show rendered screen 
pygame.event.get()



# clock for whole window

pygame.draw.rect
pygame.draw.rect





-----------
pygame.draw.rect()




        surface (Surface) -- surface to draw on

        color (Color or int or tuple(int, int, int, [int])) -- color to draw with, the alpha value is optional if using a tuple (RGB[A])

        rect (Rect) -- rectangle to draw, position and dimensions